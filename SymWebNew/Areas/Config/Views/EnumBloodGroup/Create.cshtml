@model SymViewModel.Enum.EnumBloodGroupVM
@using UIHelper.MVC
@{

    ViewBag.Title = "Edit";
    ViewBag.Location = "Edit"; 
}

@using (Html.BeginForm("Create", "EnumBloodGroup", FormMethod.Post, new { @Id = "frmBlood" }))
{
    @Html.ValidationSummary(true)
    <div class="thinbox">
    
                @Html.HiddenFor(model => model.Id)
        <div class="row">
            <label class="col-sm-3">
                @Html.LabelFor(model => model.Name)
            </label>
            <div class="col-sm-9">
                @Html.TextBoxFor(model => model.Name, new { @placeholder = "Blood Group Name", @class =" required form-control" })
            </div>
        </div>
        <div class="row">
            <label class="col-sm-3">
                @Html.LabelFor(model => model.Remarks)
            </label>
            <div class="col-sm-9">
                @Html.TextAreaFor(model => model.Remarks, new { @placeholder = "Remarks",@rows="1", @class ="form-control" })
                        @Html.ValidationMessageFor(model => model.Remarks)
            </div>
        </div>
        <div class="row">
            <label class="col-sm-3">
              
				  <button type="button" title="Save Data" onclick="pageSubmit('frmBlood');" class="sym-btn-save">&nbsp;Save</button>
            </label>
            <label class="col-sm-4">
			 <button type="button" title="Cancel Data" class=" btncancel sym-btn-cancel">&nbsp;Cancel</button>
               
            </label>
        </div>
        </div>
 
}
  <script>

      $(function () {
          valiDation("frmBlood");
          @*$('.ac').autocomplete({ source: '@Url.Action("BloodGroup", "Autocomplete", new { area = "Config" })', minLength: 2 });*@
      });
 
  </script>