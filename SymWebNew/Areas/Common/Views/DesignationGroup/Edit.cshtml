@model SymViewModel.HRM.DesignationGroupVM

@{
    ViewBag.Title = "Edit";
}

@using (Html.BeginForm("Edit", "DesignationGroup", FormMethod.Post, new { @Id = "frmDesignationGroup" }))
{
    @Html.ValidationSummary(true)

    <div class="">
        <div class="department">
            <div class="row  mt-2">
                <div class="col-md-12 ">
                    @Html.LabelFor(model => model.Code)
                </div>
                <div class="col-md-12">
                    @Html.TextBoxFor(model => model.Code, null, new { @placeholder = "Department Code", @class = "required form-control" })
                </div>
            </div>
            <div class="row  mt-2">
                <div class="col-md-12">
                    @Html.LabelFor(model => model.Name)
                </div>
                <div class="col-md-12">
                    @Html.TextBoxFor(model => model.Name, null, new { @placeholder = "Department Name", @class = "required form-control" })
                </div>
            </div>
            <div class="row mt-2">
                <label class="col-md-3">
                    @Html.LabelFor(model => model.IsActive)
                </label>
                <div class="col-md-4">
                    @Html.CheckBoxFor(model => model.IsActive, new { @class = " form-control" })
                </div>
            </div>
            <div class="row mt-2">
                <div class="col-md-12">
                    @Html.LabelFor(model => model.Remarks)
                </div>
                <div class="col-md-12">
                    @Html.TextBoxFor(model => model.Remarks, null, new { @placeholder = "Remarks", @class = " form-control" })
                </div>
            </div>

            <div class="row mt-3">
                <div class="col-sm-12">
                    <div class="reportModalbtn">
                        <button type="button" title="Edit Data" onclick="Edit();" class="Edit sym-btn-edit">&nbsp;Edit</button>
                        <button type="button" title="Save Data" onclick="pageSubmit('frmDesignationGroup');" class="Save sym-btn-save">&nbsp;Save</button>
                        <button type="button" title="Cancel Data" class=" btncancel sym-btn-cancel">&nbsp;Cancel</button>
                    </div>
                </div>
            </div>
        </div>
        @Html.HiddenFor(model => model.Id)
        @Html.HiddenFor(model => model.BranchId)



    </div>
}
<script>
    $(function () {
        Edit();
        valiDation("frmDesignationGroup");
    });
    function Edit() {
        fnReadOnly("department");
        if ($(".department").hasClass("readOnly")) {
            $(".Save").hide();
            $(".Edit").show();
        }
        else {
            $(".Save").show();
            $(".Edit").hide();
        }
    }
</script>